# 审核检查点只持久化稳定状态

> Status：已接受，已实现。

审核器逐工具进度只保留在内存；`dgoal-plan-v2` 仅在审核终态和可恢复的稳定 checkpoint 写入。审核中断后允许从头重跑本次审核，不以逐工具恢复交换会话健康度。

Pi custom entry 不进入 LLM context，但追加完整 Goal 快照会无限膨胀会话。真实会话中逐工具 checkpoint 产生数千份完整状态副本。独立审核的最终结论和稳定成功工具事实仍要持久化；中途瞬态进度不是业务事实。