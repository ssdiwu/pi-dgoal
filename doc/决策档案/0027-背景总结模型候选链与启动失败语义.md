# ADR 0027：背景总结模型候选链与启动失败语义

> Status：已决，待实现。

新增 `contextSummarizerModels` 数组配置，最多三个候选；项目配置整体优先于全局配置，不设单值兼容字段。候选按序各尝试一次，耗尽后未尝试过的当前会话模型兜底一次；全部技术失败或用户中断则清除 pending goal 并中止本次 `/dgoal`，不以无背景启动。只有成功返回“无额外背景”时可继续启动。该候选链复用隔离 Pi 与模型选择公共事实，但不复用审核器的三次同模型重试策略。
