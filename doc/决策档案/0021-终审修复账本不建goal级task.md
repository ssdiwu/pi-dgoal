# ADR 0021：终审修复账本不建 goal 级 task

> Status：已实现（vNext）。

纯 goal 级终审失败不创建 task 或免建检 phase，保持 task 永远归属 phase 的三层模型。终审失败以追加式终审修复账本留痕：每轮保存原始报告、该轮完成声明和时间；最新完整报告继续作为修复输入，历史只注入紧凑索引。修复后直接重跑 goal 终审。这样跨上下文压缩保留“为什么失败、每轮修了什么”，又不为少量终审回环引入平行任务层或额外建检。
