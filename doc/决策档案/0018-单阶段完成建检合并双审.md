# ADR 0018：单阶段完成建检合并双审

> Status：已决，待实现。

单 phase goal 不再先执行阶段建检、再执行目标终审。该 phase 的 task 已完成后，由一次独立的单阶段完成建检同时核验 phase 与 goal 的冻结验收条件；通过时原子地将 phase 与 goal 记为 done，并产出既有的结构化完成回复。多 phase goal 仍保留逐 phase 建检和最终 goal 终审：它们存在真实的中间交付与推进边界。此区分消除单 phase 下两次审核却语义重叠的等待与 token 消耗，同时不牺牲独立审核。
