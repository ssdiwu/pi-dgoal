# ADR 0022：建检行为版本隔离旧 goal

> Status：已被 ADR 0026 取代。

原决定是：新 proposal 在确认时写入显式行为版本；缺失版本的既有 goal 固定沿用旧建检链路与最新 `finalFeedback` 行为，不按 task 数或 phase 数猜测迁移。新版本才启用最小 phase、单阶段完成建检、终审归因和终审修复账本。后续决定不维护旧行为兼容，改为隔离新持久化载体；见 ADR 0026。
