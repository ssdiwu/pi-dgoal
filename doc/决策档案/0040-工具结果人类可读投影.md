# 0040 — 工具结果人类可读投影

- 状态：已采纳
- 日期：2026-07-16

## 决策

dgoal 的八个公开工具默认只显示紧凑的动作、状态或数量摘要；用户按 `Ctrl+O` 展开时，只显示来自 `content` 的人类可读文本。`details` 保留为模型、运行时和测试使用的结构化数据，不得交给宿主默认渲染，更不得以原始 JSON 或运行时对象直接展示。

## 后果

每个公开工具必须配置统一的 `renderResult`，覆盖成功、错误和进行中状态。测试须验证折叠摘要、展开文字详情，以及展开内容不依赖 `details`。
