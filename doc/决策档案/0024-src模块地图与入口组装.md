# ADR 0024：src 模块地图与入口组装

> Status：已决，待实现。

保留 `index.ts` 作为 Pi 注册、事件订阅和模块组装入口；按真实职责惰性迁入 `src/plan`、`src/goal-runtime`、`src/startup`、`src/audit`、`src/isolated-pi`、`src/tui`。`isolated-pi` 只承载隔离 Pi 调用、JSON 事件、模型注册表预检和候选选择的公共事实；auditor 与 context summarizer 各自保留不同重试和失败语义。无需预建空目录或重排 test；未来审计用量账本归 audit，实际审核模型与 Goal Repair 展示归 tui。
