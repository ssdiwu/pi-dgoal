# 40 - 版本实施方案

存放当前版本级实施方案和验收记录。受 `30-路线图` 约束。

实施某个版本切片前，在此目录建方案文档（命名 `序号-版本号-方案名.md`）。序号只在本目录当前方案内排序；归档后可从 `41` 重新起排，避免当前目录被历史编号拖累。

## 当前方案

- [v0.8.1 单一工作清单与计划保障实施方案](./44-v0.8.1-单一工作清单与计划保障实施方案.md) — **实现完成，发布验收收尾中**。Goal 下唯一 Work List、软性跨 turn 清单、Execution / Goal Check / Staged Check 单向保障、真实可选 Phase、九工具、双持久键与 session 内 Plan Run History 已进入当前代码；破坏性边界见 ADR 0051。

方案保留垂直切片、验收条件与旧状态不迁移边界；当前行为以 `doc/10-架构与运行/`、类型、测试和 ADR 0051 为准。真实模型与 TUI 状态必须在最终验收中单独报告。

## 已完成方案

- [v0.7.0 验收策略与运行预算实施方案](./43-v0.7.0-验收策略与运行预算实施方案.md) — **已实现（v0.7.0）**。在同一启动闸门由用户确认 `final_only` / `phased` 与有界 / `unbounded` 预算；主 LLM 直接提交可选背景，全局配置可受限授权隐式 `final_only` 有界启动；包含阶段进度完成、验证包、诊断审/窄确认审和预算宽限的垂直切片与验收条件。
- [v0.6.4 审核安全与主动暂停收尾方案](./42-v0.6.4-审核安全与主动暂停收尾方案.md) — **已完成自动化验证，提交/tag/push 发布收尾完成**。收敛 `dgoal_pause`、审核 checkpoint fail-closed、child 事件校验、复合凭据脱敏与版本元数据同步。
- [v0.5.2 建检反馈闭环增强实施方案](./41-v0.5.2-建检反馈闭环增强实施方案.md) — **已实现并完成自动化验证**。把阶段建检和终审未通过时检查 agent 给出的原始失败报告持久化到 `LoopGoal`，并在后续主 agent 续跑时完整钉回；同时补齐事件流活性、三态/重试、闸门锁定、裸 `/dgoal` 承接启动。切片 5/6 由自动化测试覆盖，切片 4/8 有 TUI smoke 证据；切片 5/6 的 TUI 定向补录待上游 Pi 修复 inline extension。决策见 ADR 0011 / 0012。

## 已归档方案

- [v0.2.0 TaskPlan 与建检循环实施方案](../90-归档/版本实施方案/41-v0.2.0-TaskPlan与建检循环实施方案.md) — **已实施并归档**。8 个垂直切片全部完成，95 个测试通过，独立审核 APPROVED。
- [v0.4.2 `/dgoal s` Modal 实施方案](../90-归档/版本实施方案/42-v0.4.2-dgoal-s-modal-实施方案.md) — **已完成并归档**。把 `/dgoal s` 从 notify 升级为 top-center overlay modal（Variant A 形态）。6 个垂直切片。决策见 ADR 0008。

参考 dteam 的 `40-版本实施方案/` 风格（如 `41-v0.5.0-xxx实施方案.md`）。
