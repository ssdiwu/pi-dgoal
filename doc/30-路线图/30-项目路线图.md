# 30 - 项目路线图

> dgoal 演进计划。本文件是路线图权威。设计决策见 `doc/决策档案/`，当前架构见 `10-架构与运行/`。

## 当前阶段：v0.7.0 已实现，待发布

0.6.4 是最近已发布版本（npm: pi-dgoal）。v0.7.0 的代码、测试和文档实施已完成，当前仍处于 `CHANGELOG.md` 的 `Unreleased`，尚未打 tag 或发布 npm。版本时间线见 `CHANGELOG.md`。

**待验证**（人工 smoke test，非自动化）：完整 TUI 交互（启动闸门确认 UI、dgoal_check 子进程审计、终审 rejected 回环、浮层 TUI 显示、预审 `onUpdate` 活性渲染）。

> 0.2.0 的 8 切片实现过程与拷问记录保留在下文「实现切片」「待拷问项」段，作为历史追溯；当前实现权威以 `10-架构与运行/` 为准。

## 实现切片（垂直切片，每步 TUI 可验证）

按依赖关系排序，每步端到端可验证：

1. **数据模型 + 持久化**：扩 `GoalState` 加 `plan`（Phase/Task）+ `pauseReason` + `rejectedCount` + `verification`。复用 dgoal-state custom entry。能建/存/恢复。
2. **dgoal_plan 工具**：task/phase CRUD（reducer 平移 rpiv-todo，去掉 tombstone，加 blocked）。能建 task、改状态、加 blockedBy（带环检测）。
3. **aboveEditor widget**：抄 rpiv-todo overlay。显示 phase 进度，task 默认隐藏。`Ctrl+O` 展开 task。
4. **启动闸门 + dgoal_propose**：`/dgoal` → 主代理整理 plan → `dgoal_propose` 提交 → `ctx.ui.select` 确认 UI（确认/拒绝/反馈）→ 确认进 loop。
5. **dgoal_check 阶段建检门**：phase 标 completed 走 dgoal_check（spawn 独立子进程）。通过标 completed，不过回 in_progress。
6. **dgoal_done + 终审 + 状态机**：loop_complete 改名 dgoal_done；终审走 dgoal_check；rejected/paused/pauseReason/rejectedCount×3 状态机。
7. **prompt 调和**：dgoal_plan/dgoal_check/dgoal_done guidance + before_agent_start 注入 plan 上下文 + 续跑 prompt 钉 rejected 问题。调和"先拆 plan"与"别停在计划"。
8. **loop_complete → dgoal_done 全面改名**：index.ts prompt 文本 6 处 + README/doc 同步。

## 待拷问项已全部完成（拷问 1-26）

26. **dgoal_propose 兜底（拷问 25，已决）**：主代理不调 dgoal_propose 时，降级提示重试 2 次（首次 propose 指令 + 2 次降级引导 = 3 次机会），仍无产出则中止（goal 不进 active，不污染状态机）。**不走"进 loop 自建"的纯自主兜底**——与拷问 6 否决纯自主一致。

27. **dgoal_pause 工具（拷问 26，已由 ADR 0031 覆盖）**：原决策是不加 agent 主动暂停，依赖 task blocked + 终审 ×3；在 pi-dteam 0.8 的真实 session 中复现了验收契约冲突导致 continuation 空转，证明该出口缺失会烧 token。现新增 `dgoal_pause({ reason })`：仅遇到必须由用户决策才能继续的死锁时进入 `paused(agent_blocked)`；`no_progress` 仍作兜底。一时困难仍必须先穷尽替代方案。

## v0.5.2 已实现

- **建检协议重建（事件流活性 + 闸门锁定）**：已纳入 v0.5.2。`dgoal_check` 从“最终报告导向”重建为“事件流导向”，父进程显式消费 `thinking_*`、`toolcall_*`、`text_delta`、`message_end` 等事件，产出正式建检活性状态；阶段建检不过/无结论/审核器异常时 goal 保持 `active`，但进入建检闸门锁定，不允许越阶段推进或越终审推进。设计决策见 `../决策档案/0012-阶段建检闸门锁定与事件流化审核器.md`。
- **裸 `/dgoal` 承接前文共识启动**：已纳入 v0.5.2。裸 `/dgoal` 走启动闸门（路径 B：命令层发承接信号，agent 用 `dgoal_propose` 定 objective 与可选 `contextSummary`；启动前不再运行独立 `summarizeContext`）。看状态统一用显式 `/dgoal s`。前文为空不硬启动，提示用 `/dgoal <objective>` 或先 grill。

## v0.5.4–v0.6.4 已发版（近期）

- **dgoal_propose 就绪度自检 + dgoal_done 可核对文本**（v0.5.4）：`nonGoals` / `guardrails` / `budget` 作为 plan 级信号，确认 UI 展示 L0-L3 等级与缺口；`dgoal_done` 支持 `whatChanged` / `userReview`，完成回复产出结构化可核对文本。
- **审核模型候选链与错误回退**（v0.5.7，ADR 0028）：`phaseAuditorModels` / `goalAuditorModels` 各最多 3 个有序候选，项目级整体优先、不混合来源；技术/协议异常按序单次切换，健康 fallback 按 goal 与审核范围持久复用，耗尽 `audit_error` 暂停。
- **loop 命名清理**（v0.5.4）：`LoopGoal`→`GoalState` 等类型/函数/prompt 标签统一为 goal/dgoal 命名。
- **冻结 LLM 可独立验收契约**（v0.5.8，ADR 0016）：`dgoal_propose` 要求 goal/phase 结构化 `acceptanceCriteria`，启动前语义预审把人工体验项改写到 `userReviewItems`；启动/完成 UI fail-soft。
- **vNext Goal Runtime**（v0.6.0，ADR 0022/0024/0025/0026/0027）：新持久化键 `dgoal-goal-vnext`（不兼容旧 goal）、单 phase 统一完成建检（ADR 0018）、终审三路归因（phase/goal/user_review）、src 分层（plan/audit/isolated-pi/tui/goal-runtime/startup）、背景总结候选链 fail-closed。
- **审核结论仲裁与浮层恢复**（v0.6.1/v0.6.2）：带归因 `<REJECTED>` 不再误判为部分输出；`/dgoal s` 浮层丢失后幂等重绘。
- **语义预审可观测 idle timeout 与技术/语义分离**（v0.6.3，ADR 0029）：预审从 30s 总时长超时改为 60s idle timeout，流式 `onUpdate` 活性，`technical_error`（`isError:true`）与 `rejected`（`isError:false`）分离；`proposalSemanticReviewIdleTimeoutSeconds` 可配置。
- **主动暂停与审核安全边界**（v0.6.4，ADR 0030/0031）：新增 `dgoal_pause` 主动暂停出口；审核 checkpoint 对 workspace、child 事件和成功状态 fail-closed；命令恢复上下文覆盖复合凭据脱敏。

## v0.7.0 已发版

- **v0.7.0 提案阶段确认验收、背景与启动权限**（ADR 0032/0033/0034）：同一 dgoal 在启动闸门由主 agent 推荐、用户选择 `final_only` / `phased` 与有界 / `unbounded` 预算；主 LLM 直接提交可选背景，替代启动前独立摘要。`final_only` 取消逐 phase 独立建检但保留 goal 终审，采用“诊断审 + 窄确认审”；无上限只取消预算暂停，不取消真实冲突的 `paused(agent_blocked)` 安全出口。v0.7.0 默认要求 `/dgoal`，全局用户配置可受限授权 LLM 隐式启动 `final_only` 有界 goal。

## Unreleased 已实现

- **隐式目标本地执行边界**（ADR 0035）：允许本地测试、构建、脚本、项目文件修改与本地 Git 变更；执行前策略门继续阻止已识别的仓库销毁、`.git` 破坏与外部高风险动作。
- **自然语言显式启动**（ADR 0036）：真实用户明确要求“使用/启动 dgoal”即可从冷会话创建一次性显式 pending goal，复杂计划继续经过语义预审和确认 UI，不再重复要求 `/dgoal`。

## 后续候选

- **持续计划浮层彩色化**：本次先不把 aboveEditor widget 升级为 theme-aware 彩色 widget，只统一状态字符与结构；下一版本再单列切片，专门处理层级基色渲染、跨浅/深主题可见性和 TUI 渲染回归测试。决策见 `决策档案/0009-TUI视觉编码改为层级靠颜色状态靠字符.md`。
- **运行可观测性（`/dgoal s` 运行计数）**：候选增强详细查询 Modal，展示当前 goal 的运行轮次、phase check 次数、final audit 次数、最近审核耗时与重试次数。先做不依赖 Pi core token 统计的轻量 counters；token / cost 只有在 Pi core 提供稳定接口或出现真实“烧 token 不可见”痛点后再接。
- **结构化运行时预算（Budget hard cap）**：把现有声明性 `budget` 逐步演进为可执行上限的候选，例如 `maxTurns` / `maxPhaseChecks` / `maxFinalAudits` / `maxWallClockMinutes`。触发上限后进入 paused，并给用户“已尝试什么 / 卡在哪里 / 建议下一步”。这会影响状态机和数据模型，实施前需补 ADR；没有真实空转样例前不排近期切片。
- **可观测模型操作共享骨架（ObservableOperation）**：预审（`raceIterWithIdle`）与隔离建检（`runIsolatedCheck` 的 `armIdleTimer`/`noteActivity`）当前各有一套 idle-timeout 实现。第三次同类"可观测模型操作"需求出现时，按三次原则抽取公共运行骨架（计时/倒计时/`onUpdate`/技术错误分类），见 ADR 0029 边界段。

## 暂不做 / 远期

- **跨 session goal**：dgoal 会话内单目标（AGENTS.md 边界）。跨 session 是 dteam 的范畴，不在 dgoal。
- **多 goal 池**：同上，不做。
- **循环工程（定时循环节奏）**：建检循环目前只套在“直到完成”节奏（会话内单 goal）。远期可延展到“定时循环”节奏——定时跑、每轮建检通过才算完成那轮，对应循环工程的运维场景。参考开源实践（Cobus Greyling `loop-engineering` 的 7 个 patterns）：每日巡检（daily-triage）、PR 看管（PR babysitter）、CI 清理（CI sweeper）、依赖扫描（dependency sweeper）、起草更新日志（changelog drafter）、合并后清理（post-merge cleanup）、Issue 处理（issue triage）。按“先证据后优化”，真实需求出现前不排实现切片。
- **i18n**：A-line 用户可见文案已接 `pi-di18n` 软依赖；`pi-di18n` 不在时降级内置中文。模型侧 prompt / tool schema 仍保持中文为主，不做本地化。
- **evidence 的结构化增强**：现在 evidence 是文本字段，未来可能结构化（命令+预期输出对），待真实痛点出现再做。
- **建检历史可视化**：现在 check 结果落对话/状态，未来若要"回看每次 check 结果"再考虑独立存储（建检循环心智模型刻意不建模，ADR 0006）。

## 不做（明确排除）

- 不自动 Git 提交/回滚/删除。
- 不替代项目自身测试命令。
- 不做 plan mode 的"模式切换"（dgoal 把 plan 做成 goal 内结构，不引入模式概念）。
- 不做任意深度嵌套树（用 blockedBy 依赖图涌现分解，ADR 0006）。
